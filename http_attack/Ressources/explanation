Steps:
  By following the comments in the inspect element we get some hints to send requests by using agent and referrer as options in the cURL command.


Vulenrability name: spoofing attack
Vulenrability desc:
Spoofing can be done in different environments, protocols, systems with different tools. Spoofing attack generally executed in the IT environment but there are some alternative spoofing attacks which are executed in the different areas like Geolocation, Phone Systems.

Ways to prevent vulenrability from happening:
If you are serving protected data, use cookies or OAuth tokens or something other than the Origin header to secure that data. The Access-Control-Allow-Origin header in CORS only dictates which origins should be allowed to make cross-origin requests. Don't rely on it for anything more.
